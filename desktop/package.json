{
  "name": "bno-tracker-desktop",
  "private": true,
  "version": "0.2.0",
  "description": "Desktop shell for the BNO Tracker. The app itself is ../index.html.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "sync": "node scripts/sync.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-fs": "^2.5.2",
    "@tauri-apps/plugin-opener": "^2.5.5"
  }
}
