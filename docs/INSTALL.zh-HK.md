# 喺 Mac 上面安裝 BNO 身份狀態追蹤器

🌐 [English](INSTALL.md) · **繁體中文**

呢個 app **冇買 Apple 開發者簽名**，所以第一次開嗰陣 macOS 會擋住佢，彈個
警告出嚟。呢個係正常嘅，唔係中毒。下面解釋個警告即係乜，同埋點樣過一次就
搞掂。

> 如果你唔想撳任何安全警告（呢個諗法好合理），叫 Daniel 幫你喺你部 Mac
> 開第一次就得。之後就同普通 app 一樣，double-click 就開到。

---

## 1. 安裝

1. 打開人哋send畀你嘅 `.dmg`。
2. 拖 **BNO Tracker** 入 **應用程式（Applications）** 資料夾。
3. 推出個磁碟映像。

## 2. 第一次開

第一次 double-click 係 **開唔到** 嘅。macOS 會擋住冇經過 Apple 簽名同公證
嘅 app，而呢個 app 兩樣都冇。

> **如果見到「BNO Tracker 已損毀，無法打開」** — 咁就唔關下面啲步驟事，
> 做極都冇用。呢個代表你攞到嘅係 0.2.0 版，個簽名整壞咗。同人攞返個新
> 版本就得，0.2.1 或者之後嘅版本冇呢個問題。唔好照佢講咁掉入垃圾桶，
> 換返個新嘅就得。

要咁做：

1. 打開 **系統設定 → 私隱與安全性**。
2. 掕到 **安全性** 嗰部分。
3. 你會見到一行寫住 已阻止「BNO Tracker」，隔籬有粒 **仍要打開**，撳佢。
4. 用 Touch ID 或者密碼確認。

如果搵唔到嗰行，就先 double-click 個 app 一次 — 要 macOS 擋過一次，嗰個
選項先會出現。

之後就同普通 app 一樣，double-click 就開到。

> **每個 macOS 版本嘅字眼同介面都可能唔同。** 呢啲步驟係照 macOS 26 寫嘅。
> 如果你見到嘅唔一樣，記住要搵嘅嘢 一定喺「私隱與安全性」入面，喺安全性
> 嗰段下面，而且要你試過開一次先會出現。

---

## 3. 點解會有呢個警告

Apple 收 US$99 一年先畀你個開發者簽名。冇買嘅話，任何 app — 無論安唔安全 —
都會彈同一個警告。個警告講嘅係 **邊個簽咗名**，唔係講個 app 做緊乜。

你可以自己查佢有咩權限。全部寫晒喺
[`desktop/src-tauri/capabilities/default.json`](../desktop/src-tauri/capabilities/default.json)：

- 淨係可以喺自己嗰個 Application Support 資料夾入面 讀同寫
- 可以喺瀏覽器開一條連結（畀 gov.uk 官方指引嗰條用）
- 你匯出／匯入嗰陣，可以叫系統嘅「儲存／開啟」對話框

**冇任何網絡權限。** 個 app 一個網絡請求都唔會發 — 完全 offline 都用得，
你可以熄咗 Wi-Fi 自己試。

---

## 4. 你嘅資料放喺邊

```
~/Library/Application Support/uk.bnotracker.app/
    bno-tracker.json        ← 你嘅記錄
    backups/
        2026-09-03.json     ← 每日自動備份一次，保留最近 30 個
```

點去：喺 Finder 撳 **⌘⇧G**，貼
`~/Library/Application Support/uk.bnotracker.app/`

**記得備份呢個資料夾。** Time Machine 會自動包埋佢。如果你想要一份可以
email 或者放落 USB 手指嘅，就用 app 入面嘅 **匯出 JSON**，佢會寫一個檔案
去你揀嘅位置。

**要還原：** 用 **匯入 JSON**，揀返之前匯出嘅檔，或者 `backups/` 入面
其中一個有日期嘅檔。個 app 收之前會驗一次格式，所以揀錯檔或者個檔壞咗
都只會俾佢拒絕，唔會冚咗你原本啲記錄。

---

## 5. 想移除

將個 app 由「應用程式」拖去垃圾桶。你嘅資料夾 **唔會** 一齊刪 — 如果連
記錄都想清埋，就要自己去上面嗰個路徑刪。
